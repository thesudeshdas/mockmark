{
  "name": "mockmark",
  "version": "0.1.0",
  "description": "Drop-in comments and annotations for static HTML mocks.",
  "type": "module",
  "bin": { "mockmark": "./bin/mockmark.js" },
  "main": "./src/server.js",
  "exports": {
    ".": "./src/server.js",
    "./client": "./public/mockmark/client.js",
    "./client.css": "./public/mockmark/client.css"
  },
  "files": ["bin", "src", "public", "README.md", "LICENSE"],
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check bin/mockmark.js && node --check src/server.js && node --check src/store.js"
  },
  "keywords": ["mockups", "annotations", "comments", "design-review", "static-site"],
  "author": "Sudesh Das",
  "license": "MIT",
  "engines": { "node": ">=18" }
}
